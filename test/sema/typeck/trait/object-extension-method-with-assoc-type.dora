trait Foo {
    type X;
    fn accept(v: Self::X);
}
impl Foo for Int {
    type X = Int;
    fn accept(v: Int) {}
}
impl Foo[X=Int] {
    fn another(v: Int) {}
}
fn accept(x: Foo[X=Int]) {
    x.another(10);
}
