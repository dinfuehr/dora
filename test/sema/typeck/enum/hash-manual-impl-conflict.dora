//= error
@Hash
enum Value {
    First,
    Second,
}

impl std::Hash for Value {
    fn hash(_hasher: std::Hasher) {}
}

@Hash
enum ValueWithFields {
    Value(Int32),
}

impl std::Hash for ValueWithFields {
    fn hash(_hasher: std::Hasher) {}
}
