struct Foo(Int)

impl std::traits::IndexSet for Foo {
    type Index = Int;
    type Item = Float64;

    fn set(_index: Int, _value: Float64) {}
}

fn f(array: Foo, value: Float64) {
    array(99) += value;
}
