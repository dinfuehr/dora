fn take(value: ref Int32) {}

fn take_mut(value: ref mut Int32) {}

fn main() {
    let mut value = 10i32;
    take(ref value);
    take_mut(ref mut value);
    take(ref object.field);
    take(ref tuple.0);
}
