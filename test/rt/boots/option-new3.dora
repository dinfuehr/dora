//= boots

fn main() {
    let foo = f(10);
    match foo {
        Some(x) => assert(x == 10),
        _ => unreachable[()](),
    }
}

@Optimize fn f(value: Int64): Option[Int64] {
    Some[Int64](value)
}
