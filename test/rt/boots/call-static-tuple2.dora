
fn main() {
    let foo = f();
    assert(foo.0.value == 1);
    assert(foo.1.value == 2);
}

class Value { value: Int64 }

fn f(): (Value, Value) {
    let foo = g();
    std::force_collect();
    foo
}

fn g(): (Value, Value) {
    std::force_collect();
    (Value(1), Value(2))
}
