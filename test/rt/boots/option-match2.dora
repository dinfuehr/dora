
fn main() {
    assert(is_some(Some[Foo](Foo(10))));
    assert(!is_some(None[Foo]));
}

class Foo { value: Int64 }

fn is_some(value: Option[Foo]): Bool {
    match value {
        Some(_) => true,
        _ => false,
    }
}
