//= boots
//= compile-args "--gc=copy"
//= runtime-args "--gc-stress"

let g: Float64 = {
    std::forceCollect(); 12.5
};

fn main() {
    assert(getg(Foo(7.5), 2.25) == 22.25);
}

class Foo { value: Float64 }

fn getg(x: Foo, y: Float64): Float64 {
    let result = g;
    x.value + y + result
}
