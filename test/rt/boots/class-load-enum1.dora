
fn main() {
    assert(f(Foo(Bar::A)) == Bar::A);
    assert(f(Foo(Bar::B)) == Bar::B);
}

class Foo { bar: Bar }
@Equals
enum Bar { A, B, C }

fn f(x: Foo): Bar {
    let result = x.bar;
    std::force_collect();
    result
}
