//= compile-args "--gc=copy"
//= runtime-args "--gc-stress"

let g: Int64 = {
    std::force_collect(); 12
};

fn main() {
    assert(getg(Foo(10)) == 22);
}

class Foo { value: Int64 }

fn getg(x: Foo): Int64 {
    let result = g;
    x.value + result
}
