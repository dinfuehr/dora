
fn main() {
    assert(!is_none(Some[Foo](Foo(10))));
    assert(is_none(None[Foo]));
}

class Foo { value: Int64 }

fn is_none(value: Option[Foo]): Bool {
    match value {
        None => true,
        _ => false,
    }
}
