
let g: (Int64, Bar) = {
    std::force_collect();
    (10, Bar(11))
};

fn main() {
    let local_g = getg();
    assert(local_g.0 == 10);
    assert(local_g.1.value == 11);
}

class Bar { value: Int64 }

fn getg(): (Int64, Bar) {
    g
}
