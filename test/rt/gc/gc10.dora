use std::string::Stringable;

fn main() {
    let a = A(10i32);
    let x = a.test();
    println(a.a.to_string());
    println(x.to_string());
    assert(x == 10i32);
}

class A { a: Int32 } 

impl A {
    fn test(): Int32 {
        std::force_collect();
        return self.a;
    }
}
