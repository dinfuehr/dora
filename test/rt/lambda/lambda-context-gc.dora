//= compile-args "--gc=swiper"
//= runtime-args "--gc-verify"

fn main() {
    let x = f(12);
    std::force_collect();
    assert(x() == 12);
}

fn f(a: Int64): (): Int64 {
    std::force_collect();
    ||: Int64 { a }
}
