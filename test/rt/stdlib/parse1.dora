fn main() {
  assert("1".to_int32().get_or_panic() == 1i32);
  assert("100".to_int32().get_or_panic() == 100i32);
  assert("789".to_int32().get_or_panic() == 789i32);
  assert("bla".to_int32().is_none());
}
