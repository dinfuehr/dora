fn main() {
    let x = Vec[Int]::new(1, 2, 3, 4, 5).iter().fold[Int](0, |result: Int, x: Int|: Int { result + x });
    assert(x == 15);

    let x = Array[Int]::new(1, 2, 3, 4, 5).iter().fold[Int](0, |result: Int, x: Int|: Int { result + x });
    assert(x == 15);
}
