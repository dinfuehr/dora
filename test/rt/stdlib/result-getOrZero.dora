fn main() {
  let ok = Result[Int32, String]::Ok(23i32);
  let er = Result[Int32, String]::Err("err");

  // thread 'main' panicked at 'no impl found for generic trait method call', dora-runtime/src/runtime/impls.rs
  // assert(ok.get_or_zero() == 23i32);
  // assert(er.get_or_zero() == 0i32);
}
