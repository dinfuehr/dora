fn main() {
    let x = 1.1f32;
    assert(1i32 == x.to_int32());

    let y = 213.4f32;
    assert(213i64 == y.to_int64());

    let x = 1.1f64;
    assert(1i32 == x.to_int32());

    let y = 213.4f64;
    assert(213i64 == y.to_int64());
}