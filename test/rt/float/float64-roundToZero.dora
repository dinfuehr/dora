fn main() {
  let x = Float64::infinity_negative().round_to_zero();
  assert(x == Float64::infinity_negative());

  let x = (-2.5).round_to_zero();
  assert(x == -2.0);

  let x = (-1.5).round_to_zero();
  assert(x == -1.0);

  let x = (-2.5).round_to_zero();
  assert(x == -2.0);

  let x = (-1.5).round_to_zero();
  assert(x == -1.0);

  let x = (-1.23).round_to_zero();
  assert(x == -1.0);

  let x = (-1.0).round_to_zero();
  assert(x == -1.0);

  let x = (-0.0).round_to_zero();
  assert(x == -0.0);

  let x = 0.0.round_to_zero();
  assert(x == 0.0);

  let x = 1.0.round_to_zero();
  assert(x == 1.0);

  let x = 1.23.round_to_zero();
  assert(x == 1.0);

  let x = 1.5.round_to_zero();
  assert(x == 1.0);

  let x = 2.5.round_to_zero();
  assert(x == 2.0);

  let x = Float64::infinity_positive().round_to_zero();
  assert(x == Float64::infinity_positive());

  let x = Float64::not_a_number().round_to_zero();
  assert(x.is_nan());
}
