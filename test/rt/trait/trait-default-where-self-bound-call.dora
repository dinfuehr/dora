trait Derived {
    fn derived_method(): Int64;
}

trait Base {
    fn test(): Int64 where Self: Derived {
        let lambda = ||: Int64 { self.derived_method() };
        lambda()
    }
}

class MyClass

impl Base for MyClass {}
impl Derived for MyClass {
    fn derived_method(): Int64 { 42 }
}

fn main() {
    let obj = MyClass();
    assert(obj.test() == 42);
}
