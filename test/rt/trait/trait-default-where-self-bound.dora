trait Derived {}

trait Base {
    fn test(): Int64 where Self: Derived {
        42
    }
}

class MyClass

impl Base for MyClass {}
impl Derived for MyClass {}

fn main() {
    let obj = MyClass();
    assert(obj.test() == 42);
}
