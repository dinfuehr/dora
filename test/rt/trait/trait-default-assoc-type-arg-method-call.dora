trait Bar {
    fn get_value(): Int64;
}

trait Foo {
    type T: Bar;
    fn baz(x: Self::T): Int64 {
        x.get_value()
    }
}

class MyValue {
    value: Int64
}

impl Bar for MyValue {
    fn get_value(): Int64 { self.value }
}

class MyClass

impl Foo for MyClass {
    type T = MyValue;
}

fn main() {
    let obj = MyClass();
    assert(obj.baz(MyValue(42)) == 42);
}
