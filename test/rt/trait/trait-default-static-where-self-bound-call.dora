trait Derived {
    static fn derived_method(): Int64;
}

trait Base {
    static fn test(): Int64 where Self: Derived {
        Self::derived_method()
    }
}

class MyClass

impl Base for MyClass {}
impl Derived for MyClass {
    static fn derived_method(): Int64 { 42 }
}

fn main() {
    assert(MyClass::test() == 42);
}
