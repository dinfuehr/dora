enum Foo { A, B }

fn main() {
    assert(is_a(Foo::A));
    assert(!is_a(Foo::B));
    assert(is_b(Foo::B));
    assert(!is_b(Foo::A));
}

fn is_a(x: Foo): Bool {
    x is Foo::A
}

fn is_b(x: Foo): Bool {
    x is Foo::B
}
