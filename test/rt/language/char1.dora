use std::string::Stringable;

fn main() {
    let ch = 'a';
    println(ch.to_string());

    let ch1 = 'a';
    let ch2 = 'b';

    assert(ch == ch1);
    assert(ch != ch2);
    assert(ch < ch2);

    assert(ch.to_int32() == 97i32);
    assert(ch.to_int64() == 97i64);
}
