fn main() {
    let x = Vec[Int32]::new();
    x.push(1i32);
    x.push(2i32);
    x.push(3i32);

    assert(x.pop().get_or_panic() == 3i32);
    assert(x.pop().get_or_panic() == 2i32);
    assert(x.pop().get_or_panic() == 1i32);
}
