use std::string::Stringable;

fn main() {
  assert(Array[Int32]::new().to_string() == "Array()");
  assert(Array[Int32]::new(1i32).to_string() == "Array(1)");
  assert(Array[Int32]::new(1i32, 2i32, 3i32).to_string() == "Array(1, 2, 3)");
}
