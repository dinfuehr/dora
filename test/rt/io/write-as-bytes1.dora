fn main() {
    let file = std::io::File::new("test/rt/io/write-as-bytes1.txt");
    let bytes = Array[UInt8]::new(1u8, 2u8, 3u8, 4u8);
    file.write_as_bytes(bytes);
    assert(file.read_as_bytes().get_or_panic() == bytes);
}
