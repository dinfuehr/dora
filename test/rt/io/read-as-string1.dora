fn main() {
    let content = std::io::File::new("test/rt/io/abc.txt").read_as_string().get_or_panic();
    assert(content == "abc");

    let content = std::io::File::new("unknown.txt").read_as_string();
    assert(content.is_err());
}
