use std::io::Write;

fn main() {
    let file = std::io::File::create("test/rt/io/test123.txt");
    let bytes = "hello\n".asBytes();
    let written = file.write(bytes, 0, bytes.size());
    assert(written.getOrPanic() == 6);
    file.close();
}
