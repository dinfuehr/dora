/* partial order */
trait Comparable {
  fun compareTo(other: Self) -> Int;
  // fun <  (other: Self) -> Int;
  // fun <= (other: Self) -> Int;
  // fun >  (other: Self) -> Int;
  // fun >= (other: Self) -> Int;
}

impl Comparable for Int {
  internal fun compareTo(other: Int) -> Int;
}
