class CompilationJob(let compilation_info: CompilationInfo) {
    var graph: Option[Graph] = Option[Graph]::None;

    fun run() {
        self.createGraph();
        self.optimizeGraph();
        self.assembleCode();
    }

    fun createGraph() {
        let graph = createGraph(self.compilation_info.bc);
        self.graph = Option[Graph]::Some(graph);
    }

    fun optimizeGraph() {
        // TODO
    }

    fun assembleCode() {
        // TODO
    }
}
