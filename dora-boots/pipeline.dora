class CompilationJob(let compilation_info: CompilationInfo) {
    var graph: NewOption[Graph] = NewOption[Graph]::None;

    fun run() {
        self.createGraph();
        self.optimizeGraph();
        self.assembleCode();
    }

    fun createGraph() {
        let graph = createGraph(self.compilation_info.bc);
        self.graph = NewOption[Graph]::Some(graph);
    }

    fun optimizeGraph() {
        // TODO
    }

    fun assembleCode() {
        // TODO
    }
}
