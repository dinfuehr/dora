let R0: Register = Register(0);
let R1: Register = Register(1);
let R2: Register = Register(2);
let R3: Register = Register(3);
let R4: Register = Register(4);
let R5: Register = Register(5);
let R6: Register = Register(6);
let R7: Register = Register(7);
let R8: Register = Register(8);
let R9: Register = Register(9);
let R10: Register = Register(10);
let R11: Register = Register(11);
let R12: Register = Register(12);
let R13: Register = Register(13);
let R14: Register = Register(14);
let R15: Register = Register(15);

let R16: Register = Register(16);
let R17: Register = Register(17);
let R18: Register = Register(18);
let R19: Register = Register(19);
let R20: Register = Register(20);
let R21: Register = Register(21);
let R22: Register = Register(22);
let R23: Register = Register(23);
let R24: Register = Register(24);
let R25: Register = Register(25);
let R26: Register = Register(26);
let R27: Register = Register(27);
let R28: Register = Register(28);
let R29: Register = Register(29);
let R30: Register = Register(30);

let REG_ZERO: Register = Register(32);
let REG_SP: Register = Register(33);

let REG_FP: Register = R29;
let REG_LR: Register = R30;

struct NeonRegister(value: Int32)

impl NeonRegister {
    fun encoding(): Int32 {
        self.value
    }
}

let F0: NeonRegister = NeonRegister(0);
let F1: NeonRegister = NeonRegister(1);
let F2: NeonRegister = NeonRegister(2);
let F3: NeonRegister = NeonRegister(3);
let F4: NeonRegister = NeonRegister(4);
let F5: NeonRegister = NeonRegister(5);
let F6: NeonRegister = NeonRegister(6);
let F7: NeonRegister = NeonRegister(7);
let F8: NeonRegister = NeonRegister(8);
let F9: NeonRegister = NeonRegister(9);
let F10: NeonRegister = NeonRegister(10);
let F11: NeonRegister = NeonRegister(11);
let F12: NeonRegister = NeonRegister(12);
let F13: NeonRegister = NeonRegister(13);
let F14: NeonRegister = NeonRegister(14);
let F15: NeonRegister = NeonRegister(15);

let F16: NeonRegister = NeonRegister(16);
let F17: NeonRegister = NeonRegister(17);
let F18: NeonRegister = NeonRegister(18);
let F19: NeonRegister = NeonRegister(19);
let F20: NeonRegister = NeonRegister(20);
let F21: NeonRegister = NeonRegister(21);
let F22: NeonRegister = NeonRegister(22);
let F23: NeonRegister = NeonRegister(23);
let F24: NeonRegister = NeonRegister(24);
let F25: NeonRegister = NeonRegister(25);
let F26: NeonRegister = NeonRegister(26);
let F27: NeonRegister = NeonRegister(27);
let F28: NeonRegister = NeonRegister(28);
let F29: NeonRegister = NeonRegister(29);
let F30: NeonRegister = NeonRegister(30);
let F31: NeonRegister = NeonRegister(31);

class ForwardJump(let offset: Int32, let label: Label, let kind: JumpKind)

enum JumpKind {
    Unconditional,
    Conditional(CondCode),
    NonZero(Bool, Register),
}

enum CondCode {
    EQ, // equal
    NE, // not equal
    CS,
    HS, // carry set, unsigned higher or same
    CC,
    LO, // carry clear, unsigned lower
    MI, // negative
    PL, // positive or zero
    VS, // overflow
    VC, // no overflow
    HI, // unsigned higher
    LS, // unsigned lower or same
    GE, // signed greater than or equal
    LT, // signed less than
    GT, // signed greater than
    LE, // signed less than or equal
}

class AssemblerArm64 extends Assembler {
    let buffer: AssemblerBuffer = AssemblerBuffer();

    fun ret() {
        let inst = clsx::uncondBranchReg(0b0010, 0b11111, 0, REG_LR, 0);
        self.buffer.emitInt32(inst);
    }

    fun retReg(rn: Register) {
        let inst = clsx::uncondBranchReg(0b0010, 0b11111, 0, rn, 0);
        self.buffer.emitInt32(inst);
    }

    fun br(rn: Register) {
        let inst = clsx::uncondBranchReg(0b0000, 0b11111, 0, rn, 0);
        self.buffer.emitInt32(inst);
    }

    fun blr(rn: Register) {
        let inst = clsx::uncondBranchReg(0b0001, 0b11111, 0, rn, 0);
        self.buffer.emitInt32(inst);
    }

    fun finalize(): Array[UInt8] {
        self.buffer.toArray()
    }
}

impl Register {
    fun isGpr(): Bool {
        self.value <= 30
    }

    fun isGprOrZero(): Bool {
        self.isGpr() || self == REG_ZERO
    }

    fun isGprOrSp(): Bool {
        self.isGpr() || self == REG_SP
    }

    fun encoding(): Int32 {
        assert(self.isGpr());
        self.value
    }

    fun encodingZero(): Int32 {
        assert(self.isGprOrZero());

        if self.isGpr() {
            self.value
        } else {
            31
        }
    }

    fun encodingSp(): Int32 {
        assert(self.isGprOrSp());

        if self.isGpr() {
            self.value
        } else {
            31
        }
    }
}

namespace clsx {
    import super::Register;

    @pub fun uncondBranchReg(opc: Int32, op2: Int32, op3: Int32, rn: Register, op4: Int32): Int32 {
        assert(fitsUnsigned(opc, 4));
        assert(fitsUnsigned(op2, 5));
        assert(fitsUnsigned(op3, 6));
        assert(rn.isGpr());
        assert(fitsUnsigned(op4, 5));

        0b1101011 << 25 | opc << 21 | op2 << 16 | op3 << 10 | rn.encoding() << 5 | op4
    }


    @pub fun fitsUnsigned(value: Int32, bits: Int32): Bool {
        assert(bits < 32);
        value >= 0 && value < (1 << bits)
    }
}
