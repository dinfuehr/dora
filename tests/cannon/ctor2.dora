@open class Y(let a: Int32) {}
class X(let b: Int32, a: Int32): Y(a) {}

fn cannonMain(): X {
    return X(2I, 4I);
}

fn main() {
  let x = cannonMain();

  assert(x.a == 4I);
  assert(x.b == 2I);
}
