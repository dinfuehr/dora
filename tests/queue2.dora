use std::collections::Queue;

fn main() {
    let q = Queue[Int32]();

    for i in std::range(0I, 100I) {
        q.enqueue(i);
    }

    for i in std::range(0I, 100I) {
        assert(q.dequeue() == i);
    }
}
