var a: A = A(42I);

fn main() {
    std::forceCollect();
    assert(a.val == 42I);
    std::forceCollect();
    assert(a.val == 42I);
    a = A(10I);
    std::forceCollect();
    assert(a.val == 10I);
}

fn create(): A {
    return A(10I);
}

class A(let val: Int32)
