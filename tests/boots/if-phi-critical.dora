//= boots
//= ignore

fn main() {
    assert(f(true) == 10);
    assert(f(false) == 20);
}

@optimizeImmediately fn f(x: Bool): Int64 {
    let mut result = 10;
    if x {
        result = 20
    }
    result
}
