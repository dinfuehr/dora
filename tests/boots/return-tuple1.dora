//= boots

fn main() {
    let f = makefoo(1, 2);
    assert(f.0 == 1);
    assert(f.1 == 2);
}

struct Foo(a: Int64, b: Int64)

@optimizeImmediately fn makefoo(a: Int64, b: Int64): (Int64, Int64) {
    (a, b)
}
