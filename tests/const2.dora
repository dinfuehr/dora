const i: Int32 = -1;
const l: Int64 = -2L;
const no: Bool = false;
const f: Float32 = -3.0F;
const d: Float64 = -4.0;

fun main() {
    assert(i == -1);
    assert(l == -2L);
    assert(no == false);
    assert(f == -3.0F);
    assert(d == -4.0);
}
