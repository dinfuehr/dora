fun main(): Unit {
    assert(1f32 < 3f32);
    assert((1f32 < 1f32).not());
    assert(1f32 <= 1f32);

    assert(3f32 > 1f32);
    assert((1f32 > 1f32).not());
    assert(1f32 >= 1f32);

    assert(1f64 < 3f64);
    assert((1f64 < 1f64).not());
    assert(1f64 <= 1f64);

    assert(3f64 > 1f64);
    assert((1f64 > 1f64).not());
    assert(1f64 >= 1f64);
}