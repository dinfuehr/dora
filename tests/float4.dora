fun main() {
    assert(1F < 3F);
    assert(!(1F < 1F));
    assert(1F <= 1F);

    assert(3F > 1F);
    assert(!(1F > 1F));
    assert(1F >= 1F);

    assert(1D < 3D);
    assert(!(1D < 1D));
    assert(1D <= 1D);

    assert(3D > 1D);
    assert(!(1D > 1D));
    assert(1D >= 1D);
}