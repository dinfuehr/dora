struct Foo {
    a: Int64,
    b: Int64,
    c: Int64,
}

let mut foo: Foo = Foo(100, 200, 300);

fn main() {
    foo = Foo(300, 400, 500);
    assert(foo.a == 300);
    assert(foo.b == 400);
    assert(foo.c == 500);
}
