fun main() {
  let x = "hello";
  assert(x === x);
  assert(x == "hello");
  assert("a" == "a");
  assert("a" != "b");

  assert("a" < "b");
  assert("b" > "a");

  assert("a" <= "b");
  assert("b" <= "b");

  assert("b" >= "b");
  assert("b" >= "a");

  let y = "hel" + "lo";
  assert(x == y);

  let z = "h".plus("ello");
  assert(y == z);

  assert("a".compareTo("b") < 0I);
  assert("b".compareTo("b") == 0I);
  assert("b".compareTo("a") > 0I);

  assert(!"a".equals("b"));
  assert("a".equals("a"));
}
