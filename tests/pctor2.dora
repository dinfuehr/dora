fn main() {
    let foo = Foo(123i32);
    assert(foo.a == 124i32);

    let bar = Bar(124i32);
    assert(bar.a == 125i32);
}

class_old Foo(x: Int32) {
    let a: Int32 = x+1i32;
}

class_old Bar(x: Int32) {
    var a: Int32 = x+1i32;
}
