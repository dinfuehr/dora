//= cannon

fun main() {
    let foo = Foo(123);
    assert(foo.a == 124);

    let bar = Bar(124);
    assert(bar.a == 125);
}

class Foo(x: Int32) {
    let a: Int32 = x+1;
}

class Bar(x: Int32) {
    var a: Int32 = x+1;
}
