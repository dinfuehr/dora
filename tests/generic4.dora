//= stdout "hello1"

fn main() {
  let a = A[String](foo(1i32));
  std::forceCollect();
  print(a.getx());
}

class_new A[T](x: T)

impl[T] A[T] {
    fn getx(): T {
        return self.x;
    }
}

fn foo(a: Int32): String {
  return "hello" + a.toString();
}
