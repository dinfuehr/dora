fn main() {
    let x = SomeTest[Int32](1I);
    assert(x+0I == 1I);
    assert(x-0I == 1I);
}

class SomeTest[T](let x: T) {
    fn plus(y: Int32): T = self.x;

    fn minus(y: Int32): T = self.x;
}
