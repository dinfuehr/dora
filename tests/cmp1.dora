fun main() {
  assert(1 < 2);
  assert(-4 < 2);

  assert(2 > 1);
  assert(-5 > -10);

  assert(2 == 2);
  assert(-2 != 2);

  assert(2 <= 2);
  assert(1 <= 2);

  assert(-3 >= -3);
  assert(100 >= 0);
}
