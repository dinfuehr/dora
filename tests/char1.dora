//= stdout "a\n"

fun main(): Unit {
    let ch = 'a';
    println(ch.toString());

    let ch1 = 'a';
    let ch2 = 'b';

    assert(ch == ch1);
    assert(ch != ch2);
    assert(ch < ch2);

    assert(ch.toInt32() == 97i32);
    assert(ch.toInt64() == 97i64);
}