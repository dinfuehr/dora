//= stdout "a\n"

fun main() {
    let ch = 'a';
    println(ch.toString());

    let ch1 = 'a';
    let ch2 = 'b';

    assert(ch == ch1);
    assert(ch != ch2);
    assert(ch < ch2);

    assert(ch.toInt32() == 97I);
    assert(ch.toInt64() == 97L);
}