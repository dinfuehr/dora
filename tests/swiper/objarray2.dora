//= vm-args "--gc=swiper --gc-verify"

fun main() {
    let a = Array[Foo](61);
    let b = Array[Foo](61);
    forceMinorCollect();
    forceMinorCollect();
    // a(5) = Foo(2);
    b(5) = Foo(1I);
    forceMinorCollect();
    // assert(a(5).a == 2);
    assert(b(5).a == 1I);
}

class Foo(let a: Int32)
