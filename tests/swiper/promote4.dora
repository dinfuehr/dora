//= vm-args "--gc=swiper --gc-verify"

fun main() {
  var x = Array[Foo](128);
  forceMinorCollect();
  x(0) = Foo(1I);
  x(127) = Foo(2I);
  forceMinorCollect();
  assert(x(0).a == 1I);
  assert(x(1) === nil);
  assert(x(126) === nil);
  assert(x(127).a == 2I);
}

class Foo(let a: Int32)
