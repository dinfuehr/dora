//= vm-args "--gc=swiper --gc-verify"

fun main() {
    {
        let x = Array[Object]::ofSizeUnsafe(16L * 1024L);
        std::forceCollect();
    }

    std::forceCollect();

    {
        let y = Array[Object]::ofSizeUnsafe(16L * 1024L);
        std::forceCollect();
    }

    std::forceCollect();
}
