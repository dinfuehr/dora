//= vm-args "--gc=swiper --gc-verify"

fun main() {
  let x = Foo(1I, nil);
  forceMinorCollect();
  x.next = Foo(2I, nil);
  x.next.next = Foo(3I, nil);
  forceMinorCollect();
  assert(x.a == 1I);
  assert(x.next.a == 2I);
  assert(x.next.next.a == 3I);
}

class Foo(let a: Int32, var next: Foo)
