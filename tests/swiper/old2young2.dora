//= vm-args "--gc=swiper --gc-verify"

fun main() {
  let x = Array[Foo](2);
  forceMinorCollect();
  forceMinorCollect();
  // array in old generation references object in
  // young generation.
  x(0) = Foo(2I);
  forceMinorCollect();
  assert(x(0).a == 2I);
}

class Foo(let a: Int32)
