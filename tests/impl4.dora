fn main() {
    let x = A::default();
    assert(x.a == 0I);
}

trait DefaultValue {
    @static fn default(): Self;
}

class A(let a: Int32)

impl DefaultValue for A {
    @static fn default(): A {
        return A(0I);
    }
}