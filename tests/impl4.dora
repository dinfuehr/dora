fun main() {
    let x = A::default();
    assert(x.a == A(-1).a);
}

trait DefaultValue {
    @static fun default() -> Self;
}

class A(let a: Int)

impl DefaultValue for A {
    @static fun default() -> A {
        return A(-1);
    }
}