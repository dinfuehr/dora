fun main() {
  assert(1 == A().get("hello"));

  assert(B().get(true));
  assert(!B().get(false));
}

class A {
  fun get(index: String) -> Int {
    return 1;
  }
}

class B {
  fun get(index: Bool) -> Bool {
    return index;
  }
}
