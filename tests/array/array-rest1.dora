fun main() {
    assert(number_args(1, 2) == 2L);
    assert(number_args(7) == 1L);
    assert(number_args() == 0L);
    assert(number_args(1, 2, 3, 4, 5) == 5L);
}

fun number_args(x: Int32...): Int64 {
    x.size()
}
