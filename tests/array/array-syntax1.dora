//= ignore

fun main() {
    let a = Array[Int](10);
    array_get(a, 0);
}

@new_call fun array_set(a: Array[Int], idx: Int, value: Int) {
    a(idx) = value;
}

@new_call fun array_get(a: Array[Int], idx: Int) -> Int {
    return a(idx);
}