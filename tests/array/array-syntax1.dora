fn main() {
    let a = Array[Int32]::zero(10L);
    assert(array_get(a, 0L) == 0I);

    array_set(a, 0L, 7I);
    assert(array_get(a, 0L) == 7I);
}

fn array_set(a: Array[Int32], idx: Int64, value: Int32) {
    a(idx) = value;
}

fn array_get(a: Array[Int32], idx: Int64): Int32 = a(idx);
