fun main() {
  let x = Array[Bool]::fill(3L, true);
  assert(x.contains(true));

  let x = Array[Int32]::fill(3L, 3);
  assert(x.contains(3));

  let x = Array[Int64]::fill(3L, 3L);
  assert(x.contains(3L));

  let x = Array[Float64]::fill(3L, 0.0/0.0);
  assert(x.contains(0.0/0.0));
}
