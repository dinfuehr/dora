fun main() {
  let x = arrayFill[Bool](3L, true);
  assert(arrayContains[Bool](x, true));

  let x = arrayFill[Int32](3L, 3I);
  assert(arrayContains[Int32](x, 3I));

  let x = arrayFill[Int64](3L, 3L);
  assert(arrayContains[Int64](x, 3L));

  let x = arrayFill[Double](3L, 0.0/0.0);
  assert(arrayContains[Double](x, 0.0/0.0));
}
