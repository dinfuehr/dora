fun main(): Unit {
  let x = Array[Bool]::fill(3i64, true);
  assert(x.contains(true));

  let x = Array[Int32]::fill(3i64, 3i32);
  assert(x.contains(3i32));

  let x = Array[Int64]::fill(3i64, 3i64);
  assert(x.contains(3i64));

  let x = Array[Float64]::fill(3i64, 1.0);
  assert(x.contains(1.0));
}
