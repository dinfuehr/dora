//= error nil

fun main() {
    assert(slowLength(nil) == 0L);
}

fun slowLength(x: Array[Int32]) -> Int64 {
    var entries = 0L;
    for val in x {
        entries = entries + 1L;
    }
    entries
}
