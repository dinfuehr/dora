//= cannon

fun main() {
    let q = Queue[Int]();
    assert(q.length() == 0);
    assert(q.isEmpty());
    q.enqueue(1);
    assert(q.length() == 1);
    assert(!q.isEmpty());
    q.enqueue(2);
    assert(q.length() == 2);
    assert(q.dequeue() == 1);
    assert(q.dequeue() == 2);
    assert(q.length() == 0);
    assert(q.isEmpty());
}
