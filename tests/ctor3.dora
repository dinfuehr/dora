fn main() {
    let x1 = X(1i32, 3i32);
    let x2 = X::empty();

    assert(x1.ma == x2.mb);
    assert(x1.mb == x2.ma);
}

class_old X(a: Int32, b: Int32) {
    var ma: Int32 = a;
    var mb: Int32 = b;

    @static fn empty(): X {
        X(3i32, 1i32)
    }
}
