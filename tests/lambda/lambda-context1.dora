//= vm-args "--gc=swiper --gc-verify"

fn main() {
    var x = 10;
    x = 11;
    let y = x;
    std::forceCollect();
    let fct = ||: Int64 { x };
    std::forceCollect();
    assert(x == 11);
    assert(fct() == 11);
    x = 12;
    assert(x == 12);
    assert(fct() == 12);
}
