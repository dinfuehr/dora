fun main() {
    let x = 1I.toInt64();
    let y = id(x);

    assert(x == y);
    assert(x >= y);
    assert(x <= y);
    assert(!(x > y));
    assert(!(x < y));
}

fun id(val: Int64): Int64 {
    return val;
}
