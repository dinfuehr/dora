fn main() {
    let x = SomeTest();
    assert(x+0i32 == 1.0f32);
    assert(x-0i32 == 2.0f32);
}

class_new SomeTest 

impl SomeTest {
    fn plus(y: Int32): Float32 = 1.0f32;
    fn minus(y: Int32): Float32 = 2.0f32;
}