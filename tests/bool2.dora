fun main() {
    let x = BoolArray();
    assert(x.len() == 0);

    let y = BoolArray(10, true);
    assert(y.len() == 10);
    assert(y[0] == true);
    assert(y[9] == true);

    y[0] = false;
    assert(y[0] == false);
    assert(y[1] == true);
}