fun main() {
    assert(9D.sqrt() == 3D);

    let x = 0D / 0D;
    assert(x.sqrt().isNan());

    let x = -1D;
    assert(x.sqrt().isNan());

    assert(9F.sqrt() == 3F);

    let x = 0F / 0F;
    assert(x.sqrt().isNan());

    let x = -1F;
    assert(x.sqrt().isNan());
}