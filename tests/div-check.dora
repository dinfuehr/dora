//= cannon

fun main() {
  let result = Int32::minValue() / -1;
  assert(result == Int32::minValue());

  let result = Int32::minValue() % -1;
  assert(result == 0);

  let result = Int64::minValue() / -1L;
  assert(result == Int64::minValue());

  let result = Int64::minValue() % -1L;
  assert(result == 0L);
}
