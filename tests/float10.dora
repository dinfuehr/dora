//= cannon

fun main() {
    let x = 1I;
    assert(1F == x.toFloat());

    let y = 5I;
    assert(5D == y.toDouble());

    let x = 2L;
    assert(2F == x.toFloat());

    let y = 3L;
    assert(3D == y.toDouble());
}