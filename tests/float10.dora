fun main() {
    let x = 1I;
    assert(1F == x.toFloat32());

    let y = 5I;
    assert(5D == y.toFloat64());

    let x = 2L;
    assert(2F == x.toFloat32());

    let y = 3L;
    assert(3D == y.toFloat64());
}