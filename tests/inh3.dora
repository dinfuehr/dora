fn main() {
  let a = A(Data(10i32), Data(20i32));
  std::forceCollect();

  assert(a.x.data == 10i32);
  assert(a.y.data == 20i32);
}

class_old A(let x: Data, d: Data): B(d)
@open class_old B(let y: Data)

class_old Data(let data: Int32)
