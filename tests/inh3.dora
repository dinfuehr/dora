fun main() {
  let a = A(Data(10I), Data(20I));
  std::forceCollect();

  assert(a.x.data == 10I);
  assert(a.y.data == 20I);
}

class A(let x: Data, d: Data) extends B(d)
@open class B(let y: Data)

class Data(let data: Int32)
