fn main() {
  let a = A(Data(10i32), Data(20i32));
  std::forceCollect();

  assert(a.x.data == 10i32);
  assert(a.y.data == 20i32);
}

class A(let x: Data, d: Data): B(d)
@open class B(let y: Data)

class Data(let data: Int32)
