fun main() {
  Bar(1).do();
}

class Bar(let a: int) {
  fun do() {
    Foo(self.f()).a = self.g();
    assert(a == 100);
  }

  fun f() -> int {
    assert(a == 1);
    a = 10;

    return a;
  }

  fun g() -> int {
    assert(a == 10);
    a = 100;

    return a;
  }
}

class Foo(var a: int)
