fun main() {
  Bar(1).exec();
}

class Bar(var a: int) {
  fun exec() {
    Foo(self.f()).a = self.g();
    assert(a == 100);
  }

  fun f() -> int {
    assert(a == 1);
    a = 10;

    return a;
  }

  fun g() -> int {
    assert(a == 10);
    a = 100;

    return a;
  }
}

class Foo(var a: int)
