//= error code 1
//= stderr "fatal error: locales different\n    std::fatalError (stdlib/prelude.dora:1)\n    std::Text#plus (stdlib/Text.dora:5)\n    main (tests/text/text-different-locales.dora:13)\n"

use std::Locale;
use std::Text;

fn main() {
  let en = Locale("en");
  let de = Locale("de");
  let hello = Text("hello ", en);
  let welt = Text("welt!", de);

  hello + welt;
}
