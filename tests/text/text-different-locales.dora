//= error code 1
//= stderr "fatal error: locales different\n3: std::fatalError (stdlib/prelude.dora:1)\n2: std::plus (stdlib/Text.dora:5)\n1: main (tests/text/text-different-locales.dora:13)\n"

use std::Locale;
use std::Text;

fn main() {
  let en = Locale("en");
  let de = Locale("de");
  let hello = Text("hello ", en);
  let welt = Text("welt!", de);

  hello + welt;
}
