fn main() {
    let foo = Foo(123i32);

    assert(foo.a == 123i32);
    assert(foo.b == 123i32);

    let bar = Bar(124i32);

    assert(bar.a == 124i32);
    assert(bar.b == 124i32);
}

class_old Foo(let b: Int32) {
    let a: Int32 = b;
}

class_old Bar(var b: Int32) {
    var a: Int32 = b;
}
