fun main() {
    let foo = Foo(123I);

    assert(foo.a == 123I);
    assert(foo.b == 123I);

    let bar = Bar(124I);

    assert(bar.a == 124I);
    assert(bar.b == 124I);
}

class Foo(let b: Int32) {
    let a: Int32 = b;
}

class Bar(var b: Int32) {
    var a: Int32 = b;
}
