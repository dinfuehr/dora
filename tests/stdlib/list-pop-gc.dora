//= stdout "321"

fun main(): Unit {
    let x = List[String]::new();
    x.push(1i32.toString());
    x.push(2i32.toString());
    x.push(3i32.toString());

    std::forceCollect();

    print(x.pop().getOrPanic());
    print(x.pop().getOrPanic());
    print(x.pop().getOrPanic());
}
