fun main() {
    let rnd = std::Random(0L);

    assert(rnd.nextInt32() == -1155484576I);
    assert(rnd.nextInt32() == -723955400I);
    assert(rnd.nextInt32() == 1033096058I);
    assert(rnd.nextInt32() == -1690734402I);
}
