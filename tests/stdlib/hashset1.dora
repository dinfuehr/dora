fun main() {
    let set = std::HashSet[Int32]();
    set.insert(1I);
    set.insert(10000I);
    set.insert(7I);

    assert(set.size() == 3L);
    assert(set.isEmpty().not());

    assert(set.contains(1I));
    assert(set.contains(10000I));
    assert(set.contains(7I));
    assert(!set.contains(2I));

    set.remove(10000I);

    assert(set.size() == 2L);
    assert(set.isEmpty().not());

    assert(set.contains(1I));
    assert(!set.contains(10000I));
    assert(set.contains(7I));
}
