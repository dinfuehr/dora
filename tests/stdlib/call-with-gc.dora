fn main() {
    let bar = Bar(101I);
    std::call("foo");
    assert(bar.value == 101I);
}

class Bar(let value: Int32)

fn foo() {
    let bar2 = Bar(102I);
    std::forceCollect();
    assert(bar2.value == 102I);
}
