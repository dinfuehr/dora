fun main() {
  let some = Option[Int32]::Some(23I);
  let none = Option[Int32]::None;

  // thread 'main' panicked at 'no impl found for generic trait method call', dora/src/vm/impls.rs:86:10
  // assert(some.getOrZero() == 23I);
  // assert(none.getOrZero() == 0I);
}
