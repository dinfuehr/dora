fun main() {
    let set = hashSet[Int32](1I, 10_000I, 7I);
    assert(set.length() == 3L);
    assert(set.contains(1I));
    assert(set.contains(10_000I));
    assert(set.contains(7I));
    assert(!set.contains(0I));
}
