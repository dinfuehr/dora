struct MyInt(x: Int64)

impl std::traits::Sub for MyInt {
    fn sub(rhs: MyInt): MyInt {
        MyInt(self.x - rhs.x)
    }
}

fn main() {
    let result = MyInt(5) - MyInt(1);
    assert(result.x == 4);
}
