fun main() {
  assert(get_x_or_default(A[Int](9), 10, true) == 10);
  assert(get_x_or_default(A[Int](9), 10, false) == 9);
}

fun get_x_or_default(a: A[Int], val: Int, def: Bool) -> Int {
  if def {
    return val;
  } else {
    return a.x;
  }
}

class A[T](let x: T)
