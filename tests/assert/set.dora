fun main() {
  let set = std::HashSet[Int32]();
  set.insert(1I);
  set.insert(2I);
  set.insert(3I);
  set.insert(3I);

  std::Assert::set[Int32](set).contains(1I);
  std::Assert::set[Int32](set).containsAll(1I, 2I, 3I);
  std::Assert::set[Int32](set).hasSize(3L);
  std::Assert::set[Int32](set).isNotEmpty();

  std::Assert::set[Int32](std::HashSet[Int32]())
    .hasSize(0L)
    .isEmpty();
}
