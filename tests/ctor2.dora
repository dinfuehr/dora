class X {
    var ma : Int;
    var mb : Int;

    init(a: Int, b: Int) {
        self.ma = a;
        self.mb = b;
    }
}

fun main() {
    let x = X(2, 4);

    assert(x.ma == 2);
    assert(x.mb == 4);
}