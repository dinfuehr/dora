//= output "ab\n"

fun main() {
    let x = emptyStrArray();
    assert(x.len() == 0);

    let x = strArrayWith(10, nil);
    assert(x.len() == 10);

    x[0] = "a";
    x[1] = "b";

    print(x[0]);
    println(x[1]);

    assert(x[2] === nil);
    assert(x[9] === nil);
}