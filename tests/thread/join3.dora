class MyThread extends std::Thread {
    @override fn run() {
        std::sleep(1I);
        std::forceCollect();
    }
}

fn main() {
    let thread = MyThread();
    thread.start();
    thread.join();
}
