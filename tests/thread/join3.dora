class MyThread: std::Thread {
    @override fn run() {
        std::sleep(1i32);
        std::forceCollect();
    }
}

fn main() {
    let thread = MyThread();
    thread.start();
    thread.join();
}
