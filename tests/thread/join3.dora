class MyThread extends std::Thread {
    @override fun run() {
        std::sleep(1I);
        std::forceCollect();
    }
}

fun main() {
    let thread = MyThread();
    thread.start();
    thread.join();
}
