//= vm-args "--gc=copy"

class MyThread: std::Thread {
    @override fn run() {}
}

fn main() {
    var i = 0I;

    while i < 10_000I {
        let thread = MyThread();
        thread.start();

        if i % 1_000I == 0I {
            std::forceCollect();
        }

        i = i + 1I;
    }
}
