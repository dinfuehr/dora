class MyThread() extends std::Thread {
    @override fn run() {
        nativeCalls();
    }
}

fn main() {
    let thread = MyThread();
    thread.start();

    nativeCalls();
}

fn nativeCalls() {
    var i = 0I;

    while i < 100_000I {
        std::sleep(0I);
        i = i + 1I;
    }
}