class MyThread() extends std::Thread {
    @override fun run() {
        nativeCalls();
    }
}

fun main() {
    let thread = MyThread();
    thread.start();

    nativeCalls();
}

fun nativeCalls() {
    var i = 0I;

    while i < 100_000I {
        std::sleep(0I);
        i = i + 1I;
    }
}