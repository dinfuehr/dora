//= stdout "one\ntwo\n"

import std::Thread;

class MyThread() extends Thread {
    @override fun run() {
        std::sleep(2);
        println("two");
    }
}

fun main() {
    let thread = MyThread();
    thread.start();

    println("one");
}