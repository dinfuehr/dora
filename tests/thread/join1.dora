//= stdout "one\ntwo\n"

class MyThread: std::Thread {
    @override fn run() {
        std::sleep(2I);
        println("one");
    }
}

fn main() {
    let thread = MyThread();
    thread.start();
    thread.join();
    println("two");
}
