//= output "DONE\n"

fun main() {
  do {
    foo();
  } catch x: IntArray {
    assert(x.len() == 5);
    println("DONE");
  }
}

fun foo() throws {
  do {
    throw intArrayWith(5, 1);
  } finally {
    forceCollect();
  }
}
