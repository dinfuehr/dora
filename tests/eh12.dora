//= output "DONE\n"

fun main() {
  do {
    try foo();
  } catch x: IntArray {
    assert(x.len() == 5);
    println("DONE");
  }
}

fun foo() throws {
  do {
    throw IntArray(5, 1);
  } finally {
    forceCollect();
  }
}
