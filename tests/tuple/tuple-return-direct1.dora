fun main() {
    let x = Bar(42I).tpl1();
    std::forceCollect();
    assert(x.1.value == 17I);
}

class Bar(let value: Int32) {
    fun tpl1(): (Int32, Foo) {
        assert(self.value == 42I);
        (1I, Foo(17I))
    }
}

class Foo(let value: Int32)
