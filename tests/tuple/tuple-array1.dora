fun main(): Unit {
    let arr = Array[(Int32, Int64)]::fill(10i64, (0i32, 0i64));
    assert(arr.size() == 10i64);
    let arr = Array[(Int32, Int64)]::fill(0i64, (0i32, 0i64));
    assert(arr.size() == 0i64);
}
