//= vm-args "--gc=swiper --gc-verify"

class TupleClass {
    var value: (Foo, Int64) = (Foo(0I), 0L);
}

class Foo(let value: Int32)

fun main() {
    let tpl = TupleClass();
    std::forceCollect();
    tpl.value = (Foo(4711I), 4712L);
    std::forceMinorCollect();
    assert(tpl.value.0.value == 4711I);
    assert(tpl.value.1 == 4712L);
}
