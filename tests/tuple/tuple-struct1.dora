struct Pair { x: Int32, y: Int32 }

fn main() {
    let (p1, p2) = f();
    assert((p1.x + p1.y) * (p2.x + p2.y) == 18i32);
}

fn f(): (Pair, Pair) {
    (Pair(x = 1i32, y = 2i32), Pair(x = 5i32, y = 1i32))
}
