//= vm-args "--gc=copy --disable-tlab --gc-stress"

var x: (Foo, Foo) = (Foo(0I), Foo(0I));

fun main() {
    std::forceCollect();
    x = (Foo(17I), Foo(42I));
    assert(x.0.value == 17I);
    assert(x.1.value == 42I);
    std::forceCollect();
    assert(x.0.value == 17I);
    assert(x.1.value == 42I);
}

class Foo(let value: Int32)
