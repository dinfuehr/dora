fun main() {
    assert("5".toFloat64().unwrap() == 5.0);
    assert("-127.5".toFloat64().unwrap() == -127.5);
    assert("0".toFloat64().unwrap() == 0.0);

    assert("5".toFloat32().unwrap() == 5.0F);
    assert("-127.5".toFloat32().unwrap() == -127.5F);
    assert("0".toFloat32().unwrap() == 0.0F);
}