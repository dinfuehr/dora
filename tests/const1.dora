const i: int = 1;
const l: long = 2L;
const yes: bool = true;
const zero: byte = 0B;
const f: float = 3.0F;
const d: double = 4.0;

fun main() {
    assert(i == 1);
    assert(l == 2L);
    assert(yes == true);
    assert(zero == 0B);
    assert(f == 3.0F);
    assert(d == 4.0);
}